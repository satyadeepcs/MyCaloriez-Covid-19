import { SortStatePipe } from './sort-state.pipe';

describe('SortStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortStatePipe();
    expect(pipe).toBeTruthy();
  });
});
